# BTC bubble-detection data pipeline — Phase III (acquisition + validation).
# Works on Python 3.9+ (the pins below are 3.9-compatible). Python 3.11+ is
# preferred if available. Create a venv and install:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
numpy>=1.26,<3.0
pandas>=2.1,<2.3          # 2.2.x is the last line supporting Python 3.9
scipy>=1.11,<2.0
statsmodels>=0.14,<0.15  # Phase IV: probit/logit linking + adfuller cross-check
matplotlib>=3.8,<4.0
requests>=2.31,<3.0
PyYAML>=6.0,<7.0
yfinance>=0.2.40,<0.3.0
pyarrow>=15.0            # parquet engine for the raw cache
pytest>=8.0,<9.0        # tests
